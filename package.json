{
  "name": "office-auto-mcp",
  "private": true,
  "type": "module",
  "bin": {
    "office-auto": "./bin/office-auto-cli.ts"
  },
  "scripts": {
    "mcp": "node --import tsx mcp/office-auto-server.ts",
    "cli": "node --import tsx bin/office-auto-cli.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "tsx": "^4.19.4",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^22.15.3",
    "typescript": "^5.8.3",
    "vitest": "^2.1.0"
  }
}
